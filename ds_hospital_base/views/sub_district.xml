<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <data>
    
        <!-- ds.sub_district action window -->
        <record id="ds_sub_district_action" model="ir.actions.act_window">
            <field name="name">ds.sub_district.action</field>
            <field name="type">ir.actions.act_window</field>
            <field name="res_model">ds.sub_district</field>
            <field name="view_mode">list,form</field>
            <field name="help" type="html">
                <p class="oe_view_nocontent_create">
                    <!-- Add Text Here -->
                </p><p>
                    <!-- More details about what a user can do with this object will be OK --> 
                
                </p>
            </field>
        </record>
    <!-- This Menu Item must have a parent and an action -->
    <menuitem id="sub_district_menu_act" name="sub_district" parent="ds_area_menu_categ" action="ds_sub_district_action" sequence="0"/>
    </data>
    

</odoo>
