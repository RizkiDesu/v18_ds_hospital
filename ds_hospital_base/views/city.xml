<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <data>
    
       <!-- ds.city action window -->
       <record id="ds_city_action" model="ir.actions.act_window">
        <field name="name">ds.city.action</field>
        <field name="type">ir.actions.act_window</field>
        <field name="res_model">ds.city</field>
        <field name="view_mode">list,form</field>
        <field name="help" type="html">
            <p class="oe_view_nocontent_create">
                <!-- Add Text Here -->
            </p><p>
                <!-- More details about what a user can do with this object will be OK --> 
            
            </p>
        </field>
       </record> 
    <!-- This Menu Item must have a parent and an action -->
    <menuitem id="ds_city_menu_action" name="city" parent="ds_area_menu_categ" action="ds_city_action" sequence="0"/>
    </data>
    

</odoo>
